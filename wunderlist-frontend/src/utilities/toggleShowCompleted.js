export const toggleShowCompleted = (tasks, showCompleted) => {
  console.log(`tasks in toggleShowCompleted`, tasks, showCompleted)
  // if (showCompleted === false) {
  //   return tasks.filter(entry => entry.completed !== false)
  // }
  // else {
  //   return tasks
  // }
  return tasks
}

